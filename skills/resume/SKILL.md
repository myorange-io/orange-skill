---
name: resume
description: 진행 중인 바이브코딩 프로젝트의 현재 상태를 읽어 "어디까지 했고 다음은 무엇"인지 알려주고 이어서 작업하도록 안내한다. "이어하기", "어디까지 했지", "계속하자", "오렌지 스킬 이어서" 같은 요청에 사용.
---

# Orange Skill — Resume

세션이 끊겼거나 시간이 지난 뒤 프로젝트를 이어갈 때, **지금 어디까지 왔는지** 빠르게 파악한다.
토큰을 거의 쓰지 않는 가벼운 현황 점검이다.

## 1. 현재 상태 읽기

현재 폴더에서 다음을 확인한다:

- `PLAN.md` — 있으면 전체를 읽는다. 특히 `## 진행 상황` 체크리스트.
- `git log --oneline -12` — 최근 작업 기록.
- `package.json` 존재 여부 — 앱이 scaffold 됐는지.
- `design/` 폴더 — Stitch 디자인이 있는지.

`PLAN.md`가 **없으면**: 아직 시작 전이다. "아직 기획 전이에요 — `/orange-skill:start`로
시작하세요." 라고 안내하고 끝낸다.

## 2. 현황 알려주기

`PLAN.md`가 있으면 짧게 정리해 보여준다 (길게 늘어놓지 않는다):

```
📋 [프로젝트 이름]
   완료: [끝난 항목들]
   다음: [체크리스트의 다음 미완료 항목]
   라이브: [배포됐으면 URL, 아니면 "아직 배포 전"]
```

다음 단계를 한 줄로 짚어준다. 예: "다음은 '신청 목록' 화면 구현이에요."

## 3. 이어가기

사용자에게 `/orange-skill:start`를 실행하면 멈춘 지점부터 자동으로 이어진다고 안내한다.
(`start`가 폴더 상태를 보고 알맞은 단계로 진입한다.)
